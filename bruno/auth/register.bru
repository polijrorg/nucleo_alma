meta {
  name: register
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/sign-up/email
  body: json
  auth: inherit
}

body:json {
  {
    "name": "USUARIO",
    "email": "luigi.rigoletto17@gmail.com",
    "password": "senhaSENHA"
  }
  
}

script:post-response {
  if (res.body && res.body.token) {
    bru.setVar("sessionValue", res.body.token);
  } else {
    console.log("No ID found in response");
  }
}
