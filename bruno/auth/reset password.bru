meta {
  name: reset password
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/reset-password
  body: json
  auth: inherit
}

body:json {
  {
    "token": "token",
    "newPassword": "senhamaisdificilainda"
  }
  
}
